#/bin/sh


case $1 in
 "-all")
    killall -9 gawnuts
	rm -rf ../gawnuts
	make
	mv gawnuts ../
	cd ..
	./gawnuts
    ;;
 "-kill")
	killall -9 gawnuts
	;;
 "-run")
   cd ..
   ./gawnuts
   ;;
 *)
   echo "options -run / -all / -kill "
   ;;
esac


